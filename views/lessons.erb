<div style="float: left;">
  <a href="/">Return to Dashboard</a><br />
  <h4 style="margin-top:20px;">Latest bookings...</h4>
</div>
<div style="float: right;">
  <h2>0</h2>
</div>
<div style="clear:both;"></div>
<% if @lessons[0] %>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Phone</th>
      <th>Lesson Time</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @lessons.each do |lesson| %>
    <tr>
      <td><%= lesson.first_name %> <%= lesson.last_name %></td>
      <td><%= lesson.email %></td>
      <td><%= lesson.phone %></td>
      <td><%= DateTime.parse(lesson.lesson_time).strftime("%a %e %b, %G at %l%P") %></td>
      <td><a href="/delete_lesson/<%= lesson.id %>" class="cancel_lesson_booking">Cancel</a></td>
    </tr>
    <% end %>
  </tbody>
</table>
<% else %>
<p style="margin-top:25px; color: #AAA; text-align:center;">Hooray! There are no future bookings in the system.</a></p>
<% end %>